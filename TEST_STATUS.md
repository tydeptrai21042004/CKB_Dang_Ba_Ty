# Test status — CKBuilder AgentOS v10.0.1

Validated on **15 August 2026** after the Week 5 Gemini/runtime-CI reliability patch and documentation/screenshot integration.

## Current automated checks

| Check | Result |
|---|---:|
| JavaScript syntax | **Passed** |
| AI/provider/agent regression subset | **144/144 passed** |
| Full Node.js regression suite | **347 total** |
| Passed | **346** |
| Failed | **0** |
| Skipped | **1 optional CCC integration** |
| Learning tests | **23/23 passed** |
| Basic CKB exercise models | **5/5 passed** |
| Community manifests | **Passed; 1 manifest validated** |
| Cross-implementation community vectors | **6 verified** |
| Community vector tests | **2/2 passed** |
| `npm run ci:runtime` | **Passed** |
| Clean `scripts/audit-release.sh` | **Passed** |

The single skipped Node.js test is the optional `@ckb-ccc/core` integration. That package is not installed in this validation container, so the case is recorded as skipped rather than falsely reported as passed.

`rustc` is also unavailable in this validation container. `npm run learning:check` therefore performed the deterministic Rust source checks but did not make a fresh compiler claim. Historical/local WSL Rust contract evidence remains separate from this fresh v10.0.1 Node/runtime validation.

## Week 5 Gemini regression coverage

The v10.0.1 suite specifically checks the provider failure that was observed through `POST /api/ai/application`:

- direct Gemini request uses current `systemInstruction`, `functionDeclarations`, `parametersJsonSchema`, `inlineData`, and `mimeType` fields;
- new Gemini selections default to `gemini-3.7-flash`;
- Gemini 3 requests do not inherit CKBuilder's low generic `temperature` override;
- the complete Gemini model function-call Part is replayed, including a mock opaque `thoughtSignature`;
- `functionResponse.id` and `functionResponse.name` match the preceding Gemini function call;
- tool result continuation reaches the second model turn successfully;
- upstream Gemini/provider rejection is mapped to HTTP 502;
- the sanitized provider detail does not reveal the user API key.

The provider tests use deterministic mocked responses. No real Gemini API key is stored in source, test fixtures, reports, or screenshots generated by this validation process.

## v10 AgentOS coverage retained

The full suite continues to cover:

- persistent Ed25519 agent-service identity and signed receipts;
- SQLite/WAL private agent jobs and reputation metrics;
- unsigned CKB capacity-transfer intent construction and preflight;
- exact MCP tool + argument-hash approval;
- remote MCP private-network/SSRF policy and current `server/discover` request shape;
- read-only Fiber settlement verification;
- official CKB Dev Skills grounding;
- explicit parallel-specialist multi-agent workflow DAG;
- Mission Control/Advanced Agent UI surfaces;
- credential issuance/revocation/verification, public proof, HTML/document evidence, learning, issuer/admin, webhook, and community decoder regressions from previous versions.

## Runtime CI versus release CI

Use this while the local stack is running:

```bash
npm run ci:runtime
```

A running tree may legitimately contain `.env`, `secrets/`, `node_modules/`, logs, SQLite files, and PID/state files.

Use this only against a **clean release copy/archive**:

```bash
npm run ci:local
```

`ci:local` adds `scripts/audit-release.sh`, which intentionally rejects runtime/private artifacts from a distributable release.

## Commands used

```bash
npm run syntax:check
npm run test:ai
npm test
npm run ci:runtime
bash scripts/audit-release.sh
```

See [`reports/v10.0.1-final-test-summary.txt`](reports/v10.0.1-final-test-summary.txt) for the compact machine-oriented summary and [`reports/week-05-report.md`](reports/week-05-report.md) for the Week 5 engineering report.
