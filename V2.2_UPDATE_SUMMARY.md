# CKBuilder v2.2 update summary

## Corrected

- restored `.env.example` and `.gitignore` required by the launcher and release tests;
- restored executable permissions on `run-full-project.sh`, `stop-full-project.sh`, and setup scripts;
- normalized all 22 included Rustlings foundation solutions and removed unresolved exercise markers;
- added deterministic learning validation plus optional real `rustc` compilation and test execution;
- retained honest pending status for Academy, CCC Playground, and learner-authored Cell Model evidence;
- removed dynamic `innerHTML` use from the browser code;
- added regression coverage for learning progress, the learning HTTP endpoint, dashboard workspaces, unique HTML IDs, and safe dynamic rendering.

## Improved

- replaced the basic single-page inspector with a responsive five-workspace console;
- added service/network status, protocol overview, quick tools, clearer verification results, and a commercial dashboard layout;
- added a Learning Hub with progress bars, technical milestones, pending evidence, and repository validation commands;
- added `GET /api/learning`, generated from actual repository files;
- added learning templates and a Cell Model concept guide;
- updated documentation, changelog, release audit, screenshots, and package metadata to v2.2.0.

## Validation result

```text
JavaScript syntax: passed
Learning check: 22/22 included Rust sources clean
Node tests: 81 total, 80 passed, 0 failed, 1 optional dependency test skipped
Release audit: passed
UI smoke render: passed
```

The optional CCC test was skipped because the audit environment could not install npm packages from its incomplete internal mirror. This is an environment limitation, not a detected repository failure.
